from setuptools import setup

setup(name='BiGaus_distributions',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['BiGaus_distributions'],
      zip_safe=False)
