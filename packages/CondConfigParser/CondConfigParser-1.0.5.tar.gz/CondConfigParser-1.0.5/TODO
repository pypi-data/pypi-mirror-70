-*- mode: text; fill-column: 72; -*-

* Maybe add a condconfigparser.ConditionalConfig class that reads
  variable assignments instead of raw configuration lines

* Maybe add support for integer and float (or decimal) types
