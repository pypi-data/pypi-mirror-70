from setuptools import setup

setup(name='GaussBinomial',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['GaussBinomial'],
      author='Oladokun Pelumi',
      zip_safe=False)
