from setuptools import setup

setup(name='GaussianBinomialDistributions',
      version='0.1',
      description='Gaussian Binomial distributions',
      packages=['GaussianBinomialDistributions'],
      zip_safe=False)
