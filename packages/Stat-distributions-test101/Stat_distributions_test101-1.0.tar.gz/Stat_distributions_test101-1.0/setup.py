from setuptools import setup

setup(name='Stat_distributions_test101',
      version='1.0',
      description='Gaussian distributions and Binomial Distributions',
      packages=['Stat_distributions_test101'],
      zip_safe=False)
