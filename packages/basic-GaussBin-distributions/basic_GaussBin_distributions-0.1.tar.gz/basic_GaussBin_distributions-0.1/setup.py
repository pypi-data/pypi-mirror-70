from setuptools import setup

setup(name='basic_GaussBin_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions for basic operations',
      packages=['basic_GaussBin_distributions'],
      zip_safe=False)
