from setuptools import setup

setup(name='binogaus',
      version='0.1',
      description='Binomial and Gaussian distributions',
      author = 'Niyaz B. Hashem',
      packages=['binogaus'],
      zip_safe=False)
