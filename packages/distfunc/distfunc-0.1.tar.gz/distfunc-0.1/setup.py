from setuptools import setup

setup(name='distfunc',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['distfunc'],
      author='fgrassi',
      zip_safe=False)
