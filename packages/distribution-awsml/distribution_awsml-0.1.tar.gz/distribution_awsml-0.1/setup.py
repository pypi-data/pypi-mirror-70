from setuptools import setup

setup(name='distribution_awsml',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author = 'Bidhya Shrestha',
      zip_safe=False)
