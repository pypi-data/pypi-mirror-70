from setuptools import setup

setup(name='distributions_Antonio_M',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author = 'Antonio Mayor',
      zip_safe=False)
