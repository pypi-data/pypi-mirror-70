from setuptools import setup

setup(name='distributions_BG',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_BG'],
      author = 'Antonio Gonzalez',
      zip_safe=False)
