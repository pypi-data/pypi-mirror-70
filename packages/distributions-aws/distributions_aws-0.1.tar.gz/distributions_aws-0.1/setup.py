from setuptools import setup

setup(name='distributions_aws',
      author='sameh abouelsaad',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_aws'],
      zip_safe=False)
