from setuptools import setup

setup(name='distributions_test_quandt',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_test_quandt'],
      zip_safe=False)
