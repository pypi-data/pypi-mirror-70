from setuptools import setup

setup(name='distributions_wscnl',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['distributions_wscnl'],
      zip_safe=False)
