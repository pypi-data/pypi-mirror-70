from setuptools import setup

setup(name='fariman_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['fariman_distributions'],
      zip_safe=False)
