from setuptools import setup

setup(name='favorite_probability_distributions',
      version='0.1',
      description='Includes all-time greatest hits like Gaussian and Binomial',
      packages=['favorite_probability_distributions'],
      zip_safe=False)
