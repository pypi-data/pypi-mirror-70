from setuptools import setup

setup(name='fundamental_distrib',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['fundamental_distrib'],
      author = 'AWS Course',
      zip_safe=False)
