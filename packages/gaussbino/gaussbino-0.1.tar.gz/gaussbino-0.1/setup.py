from setuptools import setup

setup(name='gaussbino',
      version='0.1',
      description='Gaussian distributions',
      packages=['gaussbino'],
      author = 'Piyush Kumar',
      author_email = '',
      zip_safe=False)
