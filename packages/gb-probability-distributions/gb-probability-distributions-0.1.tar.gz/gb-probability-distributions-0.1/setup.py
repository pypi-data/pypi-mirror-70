from setuptools import setup

setup(name='gb-probability-distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['gb-probability-distributions'],
      zip_safe=False)
