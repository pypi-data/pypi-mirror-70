from setuptools import setup

setup(name='gb_probability_distributions',
      version='0.3',
      description='Gaussian and Binomial distributions',
      packages=['gb_probability_distributions'],
      zip_safe=False)
