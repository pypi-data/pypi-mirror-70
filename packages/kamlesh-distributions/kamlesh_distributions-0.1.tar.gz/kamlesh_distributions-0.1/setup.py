from setuptools import setup

setup(name='kamlesh_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['kamlesh_distributions'],
      zip_safe=False)
