[tool.poetry]
name = "majority-judgment"
version = "0.1.0"
description = ""
authors = ["mieuxvoter contributors <contact@mieuxvoter.fr>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
