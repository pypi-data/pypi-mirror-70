from setuptools import setup

setup(name='md_prob_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['md_prob_distributions'],
      zip_safe=False)
