from setuptools import setup

setup(name='mlnd_distributions_excercise',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['mlnd_distributions_excercise'],
      zip_safe=False)
