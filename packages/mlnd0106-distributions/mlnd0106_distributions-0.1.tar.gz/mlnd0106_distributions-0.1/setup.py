from setuptools import setup

setup(name='mlnd0106_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['mlnd0106_distributions'],
      zip_safe=False)
