from setuptools import setup

setup(name='mol_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['mol_distributions'],
      author= 'Alex Poulain',
      zip_safe=False)
