from setuptools import setup

setup(name='nikhil_distributions',
      version='0.1',
      description='Gaussian distributions,Binomial distributions',
      packages=['nikhil_distributions'],
      zip_safe=False)
