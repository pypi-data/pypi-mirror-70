from setuptools import setup

setup(name='ntare-distributions',
      version='2.0',
      description='Gaussian and Binomial distributions',
      author='Ntare Guy',
      package = ['ntare-distributions'],
      zip_safe=False)
