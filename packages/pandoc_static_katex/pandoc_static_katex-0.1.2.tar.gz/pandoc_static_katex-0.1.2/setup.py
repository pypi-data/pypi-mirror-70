#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['pandocfilters']

entry_points = \
{'console_scripts': ['pandoc-static-katex = pandoc_static_katex:main']}

setup(name='pandoc_static_katex',
      version='0.1.2',
      description='A pandoc filter that renders math at build time using KaTeX.',
      author='Zahari Kassabov',
      author_email='zk261@cam.ac.uk',
      url='https://github.com/Zaharid/pandoc_static_katex',
      py_modules=['pandoc_static_katex'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>3.7',
     )
