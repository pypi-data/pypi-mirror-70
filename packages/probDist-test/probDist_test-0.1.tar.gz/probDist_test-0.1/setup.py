from setuptools import setup

setup(
    name = 'probDist_test',
    version = '0.1',
    description = 'Gaussian and binomial distributions',
    packages = ['probDist_test'],
    zip_safe = False
)
