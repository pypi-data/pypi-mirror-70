from setuptools import setup

setup(name = 'probabilibutions',
      version = '0.1',
      description = 'Gaussian and binomial distributions',
      packages = ['probabilibutions'],
      zip_safe = False)
