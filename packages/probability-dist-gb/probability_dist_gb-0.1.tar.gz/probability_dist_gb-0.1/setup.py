from setuptools import setup

setup(name='probability_dist_gb',
      version='0.1',
      description='Gaussian And Binomial distributions',
      packages=['probability_dist_gb'],
      zip_safe=False)
