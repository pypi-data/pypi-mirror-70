from setuptools import setup

setup(name='probability_dist_udct',
      version='0.1',
      description='Binomial and Gaussian distributions',
      packages=['probability_dist_udct'],
      zip_safe=False)
