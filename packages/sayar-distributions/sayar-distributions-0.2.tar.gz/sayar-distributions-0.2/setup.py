from setuptools import setup

setup(name='sayar-distributions',
      version='0.2',
      description='Gaussian distributions',
      packages=['sayar-distributions'],
      author="Sayar Banerjee",
      zip_safe=False)
