from setuptools import setup

setup(name='shd_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['shd_distributions'],
      zip_safe=False)

