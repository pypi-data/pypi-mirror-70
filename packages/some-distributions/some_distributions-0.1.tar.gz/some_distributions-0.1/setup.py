from setuptools import setup

setup(name='some_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['some_distributions'],
      author = 'Nawal Yala',
      zip_safe=False)
