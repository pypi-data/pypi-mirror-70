from setuptools import setup

setup(name='yesmkaran_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['yesmkaran_distributions'],
      zip_safe=False)
